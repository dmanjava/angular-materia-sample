export class SimpleContact {
  constructor(fid?: string,
              firstName?: string,
              lastName?: string,
              address?: string,
              city?: string,
              state?: string,
              zipCode?: string) {
  }
}
